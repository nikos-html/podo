{
  "name": "podonova-website",
  "version": "1.0.0",
  "description": "PodoNova - Profesjonalna strona podologiczna",
  "scripts": {
    "install:frontend": "cd frontend && yarn install",
    "build": "cd frontend && yarn build",
    "start": "npx serve -s frontend/build -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
